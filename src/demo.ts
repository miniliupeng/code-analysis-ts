import { app } from 'framework';

const dataLen = 3;
let name = 'iceman';

function doWell() {
  const app = 4;
  return app;
}

function getInfos(info: string) {
  const result = app.get(info);
  return result;
}
